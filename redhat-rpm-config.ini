[program]
changelog_user = Scientific Linux Auto Patch Process <SCIENTIFIC-LINUX-DEVEL@LISTSERV.FNAL.GOV>
package_config = True

# ----------------------------------------------------
[define]
dist = .sl7

# ----------------------------------------------------
[re1]
match = (Release: .*)%%{\?dist}(.*)
replace = \1.sl7\2
changelog = Modify release string to note changes

# ----------------------------------------------------
[patch1]
method = add
patch = redhat-rpm-config-recognize-scientific-linux.patch
stripe = 1
changelog = Scientific Linux is an Enterprise Linux, modify the script to know this

